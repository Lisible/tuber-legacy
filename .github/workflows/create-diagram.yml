name: Create diagram

on:
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: githubocto/repo-visualizer@main
      with:
        excluded_path: ".github"
